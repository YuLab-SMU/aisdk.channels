# aisdk.channels 0.1.0

* First release. Adds a messaging-channel runtime and adapters that
  connect `aisdk` agents to instant-messaging platforms (such as
  Feishu/Lark), including webhook handling, event processing,
  per-conversation session stores and document ingestion.
